"""Developer tools: installation checks and the palette designer.

Nothing in here is needed to *run* the instrument; these are the scripts used to
build and verify it.

    python -m tools.selftest --pipeline    check the install and run the pipeline
    python -m tools.design_palette         regenerate the colour-blind palette
"""
